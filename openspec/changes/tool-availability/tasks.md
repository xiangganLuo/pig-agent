## 1. 可用性判据机制

- [ ] 1.1 定义可用性判据声明（可选接口 `ToolAvailability` 或注册附加 `Supplier<Availability>`，返回 available + 可选 reason），不侵入现有 `@Tool` 方法签名。
- [ ] 1.2 判据求值：包 try/catch，异常→不可用（fail-safe）；无判据→默认可用；结果可缓存一次。
- [ ] 1.3 单测：可用/不可用/异常 fail-safe/无判据默认可用。

## 2. schema 过滤（不可用工具不进模型）

- [ ] 2.1 在组装传给模型的工具定义处应用过滤，剔除不可用工具。
- [ ] 2.2 收集「(工具名, 原因)」隐藏列表，挂在可查询处（只读 API / 组装结果）。
- [ ] 2.3 单测：不可用工具不出现在 schema；可用工具正常暴露；隐藏列表内容正确且不含凭据值。

## 3. 用户可见的隐藏原因

- [ ] 3.1 `/status`（及 TUI status 面板，若 tui-frontend 已落地）展示被隐藏工具及原因（变量名级，不含凭据值）。
- [ ] 3.2 至少一个内建工具接入判据示例（如 `webSearch` → `BRAVE_API_KEY`）。
- [ ] 3.3 单测：status 展示含隐藏工具+原因、不含凭据。

## 4. 验收

- [ ] 4.1 `mvn -pl pig-agent-tools -am test` 绿；权限系统既有单测回归。
- [ ] 4.2 `CLAUDE.md` 工具/权限段落补「可用性门控」。
