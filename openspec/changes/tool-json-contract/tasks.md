## 1. 分发层双层兜底（先做，消除穿透风险）

- [x] 1.1 在工具调用/分发公共路径包 try/catch，把逸出异常转成规范错误结果返回模型，异常不穿透中断回合。
- [x] 1.2 兜底同时记录 warn/error 日志（含堆栈，脱敏），保证可诊断。
- [x] 1.3 单测：工具抛异常→分发层返回规范错误、回合不中断；正常返回不受影响。

## 2. 工具返回契约 + 脱敏

- [x] 2.1 提供错误结果构造 helper（统一 `{"error"}` 形态），错误消息经脱敏（对齐 model/mcp 脱敏约定）。
- [x] 2.2 至少对高风险/已知会抛异常的存量工具对齐契约；新工具规范遵循。
- [x] 2.3 单测：错误结果不含凭据/密钥值。

## 3. 验收

- [x] 3.1 `mvn -pl pig-agent-tools -am test` 绿。
- [x] 3.2 `CLAUDE.md`/工具开发约定补「工具返回契约 + 双层兜底 + 脱敏」。
