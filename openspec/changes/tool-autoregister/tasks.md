## 1. SPI 自动发现

- [x] 1.1 定义工具提供者 SPI（`ServiceLoader` + `META-INF/services`），内建工具经其被发现。
- [x] 1.2 装配处从「逐个手动注册」改为「SPI 自动发现 + 注册」。
- [x] 1.3 单测：现有内建工具集经 SPI 被完整发现注册；新增示例工具无需改装配即被发现。

## 2. 手动兜底 + 覆盖规则 + 加载隔离

- [x] 2.1 保留手动注册路径；同名「手动覆盖自动」规则，覆盖记 INFO 审计日志。
- [x] 2.2 未预期同名重复：按规则取一并记 warn，不静默叠加。
- [x] 2.3 单个工具加载/实例化失败 try/catch 记 warn 跳过，不中断其它（fail-safe）。
- [x] 2.4 提供回退纯手动的开关/路径。
- [x] 2.5 单测：手动覆盖自动（审计）；重复不静默叠加；单工具失败被隔离。

## 3. 验收

- [x] 3.1 `mvn -pl pig-agent-tools -am test` 绿；工具集与权限/可用性/契约等既有单测回归。
- [x] 3.2 `CLAUDE.md` 扩展章节「新增工具」更新为「落文件即自动发现（含手动兜底/覆盖）」。
